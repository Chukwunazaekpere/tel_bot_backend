export interface GenerateAddressInterface {
    // data: {
        payload: {
            privateKey: string,
            publicKey: string,
            address: string,
            wif: string,
        }
    // }
    
}